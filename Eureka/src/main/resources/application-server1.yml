server:
  port: 8761

spring:
  application:
    name: EurekaServer

eureka:
  instance:
    hostname: server1
  client:
    service-url:
      defaultZone: http://server1:8761/eureka/,http://server2:8762/eureka/